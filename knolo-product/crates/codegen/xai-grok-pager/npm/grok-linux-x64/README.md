> **Knolo product component**
> This component is distributed as part of the Knolo agent product. Public installation and user workflows are documented by the parent Knolo workspace. Historical upstream identifiers may remain in internal code for provenance; required licenses and notices remain authoritative.

# @xai-official/grok-linux-x64

Platform-specific binary for [`@xai-official/knolo`](https://www.npmjs.com/package/@xai-official/knolo) on linux-x64.

Do not install this package directly. Install the main package instead:

```sh
npm install -g @xai-official/knolo
```

The main package will automatically pull the correct binary for your platform via `optionalDependencies`.
